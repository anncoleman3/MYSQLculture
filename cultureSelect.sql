SELECT users.id AS user_id
FROM users;
SELECT users.name AS user_name
FROM users;
SELECT company.company_name AS company_name
FROM company;
SELECT orders.id AS order_id
from orders;
SELECT orders.name AS order_description
from orders;
SELECT orders.totals AS order_total
from orders;

